## Init settings.
# Parameter list to optimize.
name=Envelopes
param=${name}_MA_Period
param_list=( $param )

## EA settings.
ea_strategy_default_sets ${name}.*
ea_set_neutral_trail ${name}

## Optimization settings.
value=$(input_get ${param})
ea_set_optimize_param ${param} $(( $value - 5 )) $(( $value + 5 ))

# vim: set syntax=sh:
