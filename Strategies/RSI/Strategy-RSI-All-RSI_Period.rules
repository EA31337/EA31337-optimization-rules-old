## Init settings.
# Parameter list to optimize.
name=RSI
param=${name}_Period
param_list=( $param )

## EA settings.
ea_strategy_default_sets ${name}.*

## Optimization settings.
value=$(input_get RSI_Period)
ea_set_optimize_param ${param} $(($value - 4)) $(($value + 4)) 2

# vim: set syntax=sh:
