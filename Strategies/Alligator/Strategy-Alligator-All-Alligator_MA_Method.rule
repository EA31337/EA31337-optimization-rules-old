## Init settings.
# Parameter list to optimize.
strategy=Alligator
param=${strategy}_MA_Method
param_list=( $param )
## EA settings.
ea_strategy_default_sets ${strategy}.*
ea_set_neutral_trail ${name}

## Optimization settings (F-On, 1-Min, 2-Step, 3-Max).
ea_set_optimize_param ${param} 0 3

# $ETA: 0h$
# vim: set syntax=sh:
